function getCookie() {
    S[R]++,
        S[p] = Jn.serverTimeNow(),
        S[d] = Jn.timeNow(),
        S[B] = Vn,
        S[I] = nt.getMouseMove(),
        S[y] = nt.getMouseClick(),
        S[_] = nt.getMouseWhell(),
        S[C] = nt.getKeyDown(),
        S[E] = nt.getClickPos().x,
        S[A] = nt.getClickPos().y;
    var n = S.toBuffer();
    return zn.encode(n)
}
function serverTimeNow() {
    return  parseInt(TOKEN_SERVER_TIME)
}

function getMouseClick(){
    //从0开始，循环调用可能
    for(int i=0;i<2;i++){
        second[52]++;
    }
}


function getMouseMove(){
    //从0开始，循环调用可能
    for(int i=0;i<2;i++){
        first[52]++;
    }
}

function getMouseClick(){
    //从0开始，循环调用可能
    for(int i=0;i<2;i++){
        second[52]++;
    }
}


function getClickPos(){
    return {
        x: m,
        y: I,
        trusted: y
    }
}


function timeNow() {
    var n = new Date();
    try {
        return time =  new Date(),
        time / parseInt(second[131], first[88]) >>> second[2]
    } catch (t) {
        return time = n.getTime(),
        time / parseInt(second[121], second[84]) >>> first[52]
    }
}
